<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/ce6454a</item>
        <item name="colorPrimaryDark">@color/ce6454a</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="Theme.DiDi" parent="@android:style/Theme.Light.NoTitleBar">
        <!--<item name="android:windowAnimationStyle">@style/Animation.Activity.Wuba</item>-->
        <item name="android:windowBackground">@android:color/white</item>
    </style>
    <style name="popuStyle">
        <item name="android:windowEnterAnimation">@anim/popup_anim_in</item>
        <item name="android:windowExitAnimation">@anim/popup_anim_out</item>
    </style>

</resources>
